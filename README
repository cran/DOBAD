Typical usage:

1. To update the working directory
   >svn update

2. To add files and directories
   >svn add filename
(Default behavior is to recurse on directories)

3. To delete files and directories
   >svn delete filename

4. To commit your changes
   >svn commit -m "simple message describing your changes"

